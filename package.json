{
  "name": "yona-dev-root",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "cd web && yarn build",
    "dev": "cd web && yarn dev",
    "lint": "cd web && yarn lint"
  }
}
