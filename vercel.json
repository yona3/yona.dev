{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd web && yarn install --frozen-lockfile",
  "buildCommand": "cd web && yarn build",
  "outputDirectory": "web/.next"
}
